{
  "name": "sharebeam",
  "version": "1.0.0",
  "private": true,
  "description": "Share files and text between devices: same-WiFi auto discovery (P2P), expiring links, and bidirectional rooms (P2P).",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=18.17"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "qrcode": "^1.5.4",
    "socket.io": "^4.7.5"
  },
  "optionalDependencies": {
    "ioredis": "^5.4.1"
  }
}
